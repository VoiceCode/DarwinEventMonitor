# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Darwin Event Monitor' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'SwiftyJSON', '2.4.0'
  # Pods for Darwin Event Monitor

  target 'Darwin Event MonitorTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Darwin Event MonitorUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
